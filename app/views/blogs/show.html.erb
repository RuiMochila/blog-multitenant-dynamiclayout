<% provide(:title, @blog.name) %>
<h1><%= @blog.name %></h1>

<div id="articles">
<% current_blog.articles.each do |article| %>
  <h2><%= link_to article.name, article %></h2>
  <%= simple_format(article.content) %>
<% end %>
</div>
